<Window x:Class="ChromeNet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChromeNet"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="24"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="50"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBox Name="txtUrl"></TextBox>
            <Button Grid.Column="1" Click="Button_Click" Foreground="Green">
                <Button.Content>
                    <TextBlock >
                        确定
                    </TextBlock>
                  
                </Button.Content>
            </Button>
        </Grid>
        <Border Name="borderChrome" Grid.Row="1" BorderBrush="Silver" BorderThickness="2">

        </Border>
    </Grid>
</Window>
